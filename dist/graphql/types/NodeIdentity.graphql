# import * from "Content.graphql"

type NodeIdentity {
    # Universal
    id: ID!
    ethAddress: ID    
    datProfile: ID
    datStorage: ID
    # Local node specific
    local: NodeLocal
    # Creator specific
    creator: NodeCreator    
    # Advertiser specific
    # Channel Specific
}
type NodeCreator {
    content: [Content]
}
type NodeLocal {
    localStorageReserved: Number
    downloadRateCap: Number
    uploadRateCap: Number
    maxUploadCap: Number
}