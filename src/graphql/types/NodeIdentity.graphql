# import * from "Content.graphql"

type NodeIdentity {
    # Universal
    id: ID
    ethAddress: ID    
    datProfile: ID
    datStorage: ID
    # Local node specific
    local: NodeIdentityLocal
    # Content specific
    content: NodeIdentityContent
    # Advertiser specific
    # Channel Specific
}
type NodeIdentityContent {
    # Staked content = uploaded/owned by user
    staked: [VideoContent]
    # Hosted content = downloaded/re-hosted by user
    hosted: [VideoContent]
}
type NodeIdentityLocal {
    localStorageReserved: Int
    downloadRateCap: Int
    uploadRateCap: Int
    maxUploadCap: Int
}