# import * from "Content.graphql"

type NodeIdentity {
    # Universal
    id: ID!
    ethAddress: ID    
    datProfile: ID
    datStorage: ID
    # Local node specific
    local: NodeIdentityLocal
    # Creator specific
    creator: NodeIdentityContentCreator
    # Advertiser specific
    # Channel Specific
}
type NodeIdentityContentCreator {
    content: [VideoContent]
}
type NodeIdentityLocal {
    localStorageReserved: Int
    downloadRateCap: Int
    uploadRateCap: Int
    maxUploadCap: Int
}